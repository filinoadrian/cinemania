<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="entity"
            type="com.far_sstrwnt.cinemania.model.Entity" />

        <variable
            name="eventListener"
            type="com.far_sstrwnt.cinemania.ui.common.EventActionsHandler" />

        <variable
            name="tv"
            type="com.far_sstrwnt.cinemania.model.TvEntity" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="12dp"
        android:paddingVertical="12dp"
        android:onClick="@{() -> eventListener.openDetail(entity.TV, tv.id)}">

        <ImageView
            android:id="@+id/tv_poster"
            android:layout_width="92dp"
            android:layout_height="138dp"
            android:scaleType="fitXY"
            android:contentDescription="TV Poster"
            app:poster="@{tv.posterPath}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <TextView
            android:id="@+id/tv_name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="@{tv.name}"
            android:textColor="#1F75FE"
            android:textSize="24sp"
            app:layout_constraintStart_toEndOf="@id/tv_poster"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Avengers: Infinity War"/>

        <TextView
            android:id="@+id/tv_rating"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:text="@{Float.toString(tv.voteAverage)}"
            android:textSize="24sp"
            app:layout_constraintStart_toEndOf="@id/tv_poster"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            tools:text="9.0"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>